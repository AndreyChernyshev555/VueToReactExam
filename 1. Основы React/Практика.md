Перепиши следующую пару из родительского и дочернего Vue компонента на React.
Будет ли какое-то отличие при разных версиях React?

```vue
<template>
  <h1 v-if="needTitle">Добро пожаловать!</h1>
  <div>{{ message }}</div>
  <div class="flex-column">
    <div class="flex-column" v-for="item in arrayData" :key="item.id">
      <div>{{ item.name  }}</div>
      <div>{{ item.price }}</div>
    </div>
  </div>
</template>

<script setup>
  defineProps(['needTitle', 'message', 'arrayData']);
</script>

<style scoped>
  .flex-column {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 1em;
  }
</style>

<template>
  <div id="app">
    <SimpleComponent
      need-title
      message="Прайс-лист:"
      :array-data="array"
    />
  </div>
</template>

<script setup>
import SimpleComponent from './SimpleComponent.vue'
const array = [
  {
    id: 0,
    name: 'GeForce RTX 5060',
    price: '299 USD',
  },
  {
    id: 1,
    name: 'GeForce RTX 5050',
    price: '249 USD',
  },
  {
    id: 2,
    name: 'Radeon RX 9060 XT 8 GB',
    price: '299 USD',
  },
];
</script>

<style>
#app {
  font-family: Arial, sans-serif;
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
}
</style>
```
